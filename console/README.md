# console

Placeholder. The console client's tech stack is not yet decided and no real
code belongs here yet — it is owned by unit U3 ("Console (thin client)"),
which generates its models from `/schemas/*.schema.json` and contains no
business logic of its own (see `docs/SRS.md` §15, `docs/TRS.md` §15–16).
